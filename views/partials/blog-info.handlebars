<div class="card">
    <div class="card-header">
       <h4><a class="link" href="/blog/{{id}}">{{blog_header}}</a></h4>
        <p class="right">Posted by {{user.username}} on {{format_date created_at}}</p>
        
    </div>
        <p class="card-body">{{description}}</p>
</div>